var e = {
    session: [`session`],
    portfolios: [`portfolios`],
    userSettings: [`user-settings`],
    subscription: [`subscription`, `current`],
    coachAssignment: [`coach`, `assignment`],
    coachProfile: [`coach`, `profile`],
    dashboard: (e, t) => [`dashboard`, e ? ? `default`, t ? ? !0],
    dashboardState: (e, t) => [`dashboard`, `state`, e ? ? `default`, t ? ? !0],
    activityStreaks: e => [`dashboard`, `activity-streaks`, e ? ? `default`],
    goals: [`goals`],
    tradingPlan: [`trading-plan`],
    calendar: (e, t, n) => [`calendar`, e, t, n],
    journalEntries: [`journal`, `entries`],
    analyticsOverview: (e, t) => [`analytics`, `overview`, e, t],
    strategyBreakdown: e => [`analytics`, `strategies`, e],
    marketQuotes: e => [`market-quotes`, ...e],
    tradeHighlight: (e, t, n) => [`trades`, `highlight`, e, t, n],
    brokerIcons: [`broker-icons`],
    brokerAccounts: [`broker`, `accounts`],
    portfolioStats: e => [`portfolio`, `stats`, e],
    monthlyPnl: (e, t) => [`analytics`, `monthly-pnl`, e, t],
    tradesFilterKey: e => [`trades`, e.scope, e.startDate ? ? ``, e.endDate ? ? ``, e.useNetPnl ? ? !0, e.side ? ? ``, e.search ? ? ``, e.tradeType ? ? ``, e.outcome ? ? ``, e.assetClass ? ? ``, e.strategy ? ? ``],
    mentalEntries: (e, t) => [`mental-entries`, e, t],
    backtestStrategies: [`backtest`, `strategies`],
    backtestRuns: [`backtest`, `runs`],
    backtestRun: e => [`backtest`, `run`, e],
    infiniteTrades: e => [`trades`, `infinite`, e.scope, e.startDate ? ? ``, e.endDate ? ? ``, e.useNetPnl ? ? !0, e.side ? ? ``, e.search ? ? ``, e.tradeType ? ? ``, e.outcome ? ? ``, e.assetClass ? ? ``, e.strategy ? ? ``, e.sortBy ? ? ``, e.sortOrder ? ? ``, e.perPage ? ? 0],
    agentLibrary: [`agents`, `library`],
    agentSubscriptions: [`agents`, `subscriptions`],
    agentCredits: [`agents`, `credits`],
    discoverPortfolios: e => [`discover`, `portfolios`, e ? ? `all`],
    arenaModels: [`arena`, `models`],
    arenaLeaderboard: [`arena`, `leaderboard`],
    newsInfinite: [`news`, `infinite`],
    newsArticle: e => [`news`, `article`, e],
    aiHistory: e => [`ai`, `history`, e ? ? `default`],
    aiConversations: [`ai`, `conversations`],
    aiConversation: e => [`ai`, `conversation`, e],
    aiMemory: [`ai`, `memory`],
    marketBrief: [`daily-intelligence`, `market-brief`],
    setupAlert: [`daily-intelligence`, `setup-alert`]
};
export {
    e as t
};